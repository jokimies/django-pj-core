============
Installation
============

At the command line::

    $ easy_install django-pj-core

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-pj-core
    $ pip install django-pj-core
